import { Database } from 'bun:sqlite'; const db = new Database('src/server/sqlite.db'); db.run("CREATE TABLE IF NOT EXISTS elearning_questions (id INTEGER PRIMARY KEY AUTOINCREMENT, session_id INTEGER NOT NULL REFERENCES elearning_sessions(id) ON DELETE CASCADE, question TEXT NOT NULL, options TEXT NOT NULL, correct_answer INTEGER NOT NULL, created_at TEXT DEFAULT (datetime('now')));"); db.run("CREATE TABLE IF NOT EXISTS elearning_quiz_submissions (id INTEGER PRIMARY KEY AUTOINCREMENT, session_id INTEGER NOT NULL REFERENCES elearning_sessions(id) ON DELETE CASCADE, student_id INTEGER NOT NULL REFERENCES students(id) ON DELETE CASCADE, grade INTEGER NOT NULL, created_at TEXT DEFAULT (datetime('now')));"); console.log('Tables created');
