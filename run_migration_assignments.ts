import { Database } from 'bun:sqlite'; const db = new Database('src/server/sqlite.db'); db.run("CREATE TABLE IF NOT EXISTS elearning_assignments (id INTEGER PRIMARY KEY AUTOINCREMENT, session_id INTEGER NOT NULL, title TEXT NOT NULL DEFAULT '', description TEXT NOT NULL DEFAULT '', due_date TEXT, file_url TEXT NOT NULL DEFAULT '', created_at TEXT DEFAULT (datetime('now')));"); db.run("CREATE TABLE IF NOT EXISTS elearning_submissions (id INTEGER PRIMARY KEY AUTOINCREMENT, assignment_id INTEGER NOT NULL REFERENCES elearning_assignments(id) ON DELETE CASCADE, student_id INTEGER NOT NULL REFERENCES students(id) ON DELETE CASCADE, file_url TEXT, submitted_at TEXT DEFAULT (datetime('now')), grade INTEGER, feedback TEXT, graded_by INTEGER, graded_at TEXT);"); console.log('Assignments tables created successfully!');
